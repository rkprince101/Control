name: control_core
description: Platform-independent rule engine for Control. No Flutter, no dart:io.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
