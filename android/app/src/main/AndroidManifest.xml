<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Screen time and pickups on the Insights screen. Granted from a system
         settings page, not a runtime dialog. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Map tiles for the place picker, and nothing else. Flutter only adds
         this to debug builds, so a release build would show an empty map. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The weekly report notification. Asked for at runtime on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Re-arm blocks after a reboot; without it a restart is a free bypass. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Reads the hardware step counter for Steps conditions. Requested at
         runtime; the sensor keeps counting whether or not the app is running. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />

    <!-- One foreground fix when the user taps Check in at a place condition.
         Foreground only: no background location, no geofences, nothing tracked
         while the app is closed. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <application
        android:label="Control"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- The block screen. Its own task, kept out of recents so the blocked
             app cannot be reached by swiping back to it. -->
        <activity
            android:name=".enforcement.BlockOverlayActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=".enforcement.block"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen" />

        <!-- Fires a habit channel from an NFC tag, QR code, or home-screen
             shortcut. Finishes immediately without showing a screen. -->
        <activity
            android:name=".shortcuts.ShortcutTriggerActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="control" android:host="shortcut" />
            </intent-filter>
        </activity>

        <!-- Fires a habit channel from an automation app (Tasker, MacroDroid). -->
        <receiver
            android:name=".shortcuts.ShortcutReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.control.action.SHORTCUT" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widget: today's numbers without opening the app. -->
        <receiver
            android:name=".surface.ControlWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_control_info" />
        </receiver>

        <!-- Weekly summary alarm. -->
        <receiver
            android:name=".surface.WeeklyReportReceiver"
            android:exported="false" />

        <!-- Quick Settings tile. Read-only: a tile that could unblock would be
             a bypass reachable from the lock screen. -->
        <service
            android:name=".surface.ControlTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Re-asserts the cached plan after a restart or an app update. -->
        <receiver
            android:name=".enforcement.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Holds device admin so Android refuses to uninstall the app. -->
        <receiver
            android:name=".enforcement.ControlDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Foreground-app detection. The only signal a normal Android app gets
             that is fast enough to block on. -->
        <service
            android:name=".enforcement.ControlAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets the app picker list launchable apps without holding
             QUERY_ALL_PACKAGES, which Play restricts to a narrow set of uses. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
